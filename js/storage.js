import {uid,validDate} from './utils.js';
const DATABASE='sahod-payroll-v1';let db,revision=0;
export async function openDatabase(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DATABASE,1);req.onupgradeneeded=()=>req.result.createObjectStore('workspace');req.onsuccess=()=>{db=req.result;resolve()};req.onerror=()=>reject(req.error)})}
export async function loadState(){return new Promise((resolve,reject)=>{const r=db.transaction('workspace').objectStore('workspace').get('state');r.onsuccess=()=>{revision=r.result?.revision||0;resolve(r.result?.data??null)};r.onerror=()=>reject(r.error)})}
export async function saveState(state){const data=structuredClone(state);return new Promise((resolve,reject)=>{const tx=db.transaction('workspace','readwrite');const store=tx.objectStore('workspace');const r=store.get('state');r.onsuccess=()=>{if((r.result?.revision||0)!==revision){tx.abort();return}store.put({data,revision:revision+1},'state')};tx.oncomplete=()=>{revision++;resolve()};tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(Error('This workspace changed in another tab. Reload before saving.'))})}
export function audit(state,action,entity,oldValue=null,newValue=null){state.audit.unshift({id:uid('AUD'),timestamp:new Date().toISOString(),user:state.settings.operator||'Administrator',action,entity,oldValue:structuredClone(oldValue),newValue:structuredClone(newValue)})}
export function validateBackup(data){if(data?.schema!==1)throw Error('Unsupported backup format.');for(const k of ['employees','attendance','payrolls','holidays','loans','audit','rules','closures'])if(!Array.isArray(data[k]))throw Error(`Missing ${k} records.`);if(!data.settings?.company)throw Error('Missing company settings.');if(data.employees.length>20000||data.attendance.length>500000)throw Error('Backup is too large for this prototype.');for(const k of ['employees','attendance','payrolls','holidays','loans']){const ids=data[k].map(x=>x.id);if(ids.some(x=>typeof x!=='string'||!/^[A-Za-z0-9_-]{1,100}$/.test(x))||new Set(ids).size!==ids.length)throw Error(`Invalid or duplicate ${k} identifiers.`)}for(const e of data.employees){if(!Array.isArray(e.statusHistory)||!validDate(e.hireDate)||!e.firstName||!e.lastName||!Array.isArray(e.rateHistory))throw Error('Invalid employee record.')}for(const a of data.attendance)if(!validDate(a.date)||!data.employees.some(e=>e.id===a.employeeId))throw Error('Invalid attendance record.');for(const h of data.holidays)if(!validDate(h.date)||typeof h.name!=='string')throw Error('Invalid holiday record.');for(const p of data.payrolls){if(!validDate(p.payDate)||!validDate(p.period?.from)||!validDate(p.period?.to)||!p.company||!Array.isArray(p.ruleSnapshots))throw Error('Invalid payroll dates or snapshots.');if(!Array.isArray(p.lines)||!['Draft','Calculated','Reviewed','Approved','Paid','Voided'].includes(p.status))throw Error('Invalid payroll record.');for(const l of p.lines)for(const k of ['gross','net','tax','totalDeductions'])if(!Number.isSafeInteger(l[k]))throw Error('Invalid payroll monetary amount.')}return data}
export function backup(state){return JSON.stringify({...state,exportedAt:new Date().toISOString()},null,2)}
